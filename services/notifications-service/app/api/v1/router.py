from fastapi import APIRouter

router = APIRouter()

# from app.api.v1 import notifications, email, push
# router.include_router(notifications.router, prefix="/notifications", tags=["notifications"])
