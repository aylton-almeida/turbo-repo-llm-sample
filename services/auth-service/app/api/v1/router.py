from fastapi import APIRouter

router = APIRouter()

# from app.api.v1 import auth, tokens
# router.include_router(auth.router, prefix="/auth", tags=["auth"])
# router.include_router(tokens.router, prefix="/tokens", tags=["tokens"])
